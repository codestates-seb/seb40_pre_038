
spring:
  h2:
    console:
      enabled: true
      path: /h2  # Context path
  datasource:
    url: jdbc:h2:mem:StackOverFlow  # JDBC URL
  jpa:
    hibernate:
      ddl-auto: create  # ??? ?? ??
    show-sql: true  # SQL ?? ??